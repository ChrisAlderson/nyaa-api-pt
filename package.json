{
  "name": "nyaa-api-pt",
  "version": "2.0.0",
  "description": "A nyaa.pantsu.cat API wrapper for NodeJS.",
  "dependencies": {
    "debug": "^3.1.0",
    "got": "^7.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "coveralls": "^3.0.0",
    "eslint": "^4.11.0",
    "eslint-config-vixo": "^2.0.1",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0"
  },
  "license": "MIT",
  "scripts": {
    "coveralls": "cat ./coverage/lcov.info | node ./node_modules/.bin/coveralls",
    "lint": "./node_modules/.bin/eslint {examples,test}/** *.js",
    "test": "./node_modules/.bin/nyc --reporter=text --reporter=lcov ./node_modules/.bin/mocha --timeout 15000 --exit --reporter spec test/**"
  },
  "engines": {
    "node": ">=6.3.0"
  },
  "keywords": [
    "nyaa",
    "anime",
    "torrent",
    "torrents",
    "api"
  ],
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ChrisAlderson/nyaa-api-pt.git"
  }
}
